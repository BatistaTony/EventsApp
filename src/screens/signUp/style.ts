import styled from 'styled-components/native';
import {CustomScreen} from '../../MainStyles/mainStyle';
import Colors from './../../constants/colors';

export const ScreenSignUp = styled(CustomScreen)`
  background: ${Colors.bg1};
`;
